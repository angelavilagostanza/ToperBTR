<?xml version="1.0" encoding="UTF-8"?>
<!--
  Bloquea acceso HTTP directo a /dal/ (mismo razonamiento que /include/web.config).
-->
<configuration>
  <system.webServer>
    <handlers>
      <clear />
    </handlers>
  </system.webServer>
</configuration>
